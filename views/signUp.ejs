<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up | Saraha</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        fadeIn: "fadeIn 1s ease-out",
                        slideUp: "slideUp 0.5s ease-out",
                    },
                    keyframes: {
                        fadeIn: {
                            "0%": { opacity: 0 },
                            "100%": { opacity: 1 },
                        },
                        slideUp: {
                            "0%": { transform: "translateY(20px)", opacity: 0 },
                            "100%": { transform: "translateY(0)", opacity: 1 },
                        },
                    },
                    boxShadow: {
                        'deep': '0px 10px 30px rgba(0, 0, 0, 0.2)',
                    }
                },
            },
        };
    </script>
</head>

<body
    class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-r from-indigo-500 to-purple-600 px-4">
    <% if (error) { %>
        <div id="error-alert" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg relative mb-4" role="alert">
            <strong class="font-bold">Oops! </strong>
            <span class="block sm:inline"><%= error %></span>
        </div>

        <script>
            // Remove the error message after 5 seconds
            setTimeout(() => {
                const errorAlert = document.getElementById("error-alert");
                if (errorAlert) {
                    errorAlert.style.opacity = "0";  // Fade out effect
                    setTimeout(() => errorAlert.remove(), 500); // Remove from DOM
                }
            }, 5000);
        </script>
    <% } %>
    <!-- Sign Up Container -->
    <div class="w-full max-w-md p-8 bg-white rounded-2xl shadow-deep animate-fadeIn">
        <!-- Logo -->
        <div class="flex justify-center">
            <img src="https://cdn-icons-png.flaticon.com/512/295/295128.png" alt="Saraha Logo" class="w-16 mb-3">
        </div>

        <!-- Sign-Up Form -->
        <h2 class="text-3xl font-extrabold text-center text-gray-800 mb-6 animate-slideUp">Create an Account</h2>

        <form class="animate-slideUp space-y-4" method="POST" action="/">
            <div>
                <label class="block text-gray-700 font-semibold">Username</label>
                <input type="text" placeholder="Choose a username"
                    class="w-full px-4 py-3 mt-1 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all duration-300 bg-gray-50"
                    name="userName">

            </div>

            <div>
                <label class="block text-gray-700 font-semibold">Email</label>
                <input type="email" placeholder="Enter your email"
                    class="w-full px-4 py-3 mt-1 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all duration-300 bg-gray-50"
                    name="email">
            </div>

            <div>
                <label class="block text-gray-700 font-semibold">Password</label>
                <input type="password" placeholder="Create a password"
                    class="w-full px-4 py-3 mt-1 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all duration-300 bg-gray-50"
                    name="password">
            </div>

            <div>
                <label class="block text-gray-700 font-semibold">Confirm Password</label>
                <input type="password" placeholder="Confirm your password"
                    class="w-full px-4 py-3 mt-1 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all duration-300 bg-gray-50"
                    name="cPassword">
            </div>

            <button type="submit"
                class="w-full bg-indigo-500 text-white py-3 rounded-xl font-semibold hover:bg-indigo-600 transition-transform duration-300 hover:scale-105 shadow-lg">
                Sign Up
            </button>

            <p class="text-center text-gray-600 mt-4">Already have an account?
                <a href="/login" class="text-indigo-500 hover:underline font-semibold">Login</a>
            </p>
        </form>
    </div>

    <%- include('footer.ejs') %>
</body>

</html>