<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Sent | Saraha</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        fadeIn: "fadeIn 0.8s ease-out",
                    },
                    keyframes: {
                        fadeIn: {
                            "0%": { opacity: 0 },
                            "100%": { opacity: 1 },
                        }
                    },
                },
            },
        };
    </script>
</head>

<body class="flex flex-col min-h-screen justify-center items-center bg-gradient-to-r from-blue-500 to-purple-600 px-4">

    <!-- Message Sent Box -->
    <div class="w-full max-w-md p-6 bg-white rounded-2xl shadow-xl text-center animate-fadeIn">
        <h2 class="text-3xl font-bold text-gray-800">✅ Message Sent!</h2>
        <p class="text-gray-500 mt-2">Your anonymous message has been delivered successfully.</p>

        <!-- Sign Up Section -->
        <div class="mt-6 p-4 bg-gray-100 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold text-gray-700">Get Your Own Anonymous Inbox!</h3>
            <p class="text-gray-600 text-sm mt-1">Sign up to receive and manage anonymous messages.</p>
            <a href="/"
                class="block w-full bg-blue-500 text-white py-2 mt-4 rounded-lg font-semibold hover:bg-blue-600 transition-transform duration-300 hover:scale-105 shadow-md">
                ✍️ Sign Up Now
            </a>
        </div>

        <!-- Back to Home Button -->
        <a href="/" class="mt-4 inline-block text-blue-500 font-semibold hover:underline">⬅ Back to Home</a>
    </div>

    <!-- Footer -->
    <footer class="w-full">
        <%- include('footer.ejs') %>
    </footer>

</body>

</html>