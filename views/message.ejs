<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Send Message | Saraha</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    boxShadow: {
                        'deep': '0px 10px 30px rgba(0, 0, 0, 0.2)',
                    }
                },
            },
        };
    </script>
</head>

<body class="bg-gradient-to-r from-blue-500 to-purple-600 min-h-screen flex flex-col items-center justify-center px-4">

    <!-- Message Container -->
    <div class="w-full max-w-lg bg-white p-10 rounded-3xl shadow-2xl text-center">
        <h2 class="text-3xl font-bold text-gray-900 mb-6">💌 Send an Anonymous Message</h2>

        <!-- Message Form -->
        <form class="space-y-6" action="/message/<%= user._id %>/messageSent" method="POST">
            <div class="text-left">
                <label class="block text-gray-700 font-medium text-lg mb-2">Your Message</label>
                <textarea rows="6" placeholder="Write your message here..."
                    class="w-full px-5 py-4 text-gray-700 border rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all duration-300 resize-none shadow-sm text-lg"
                    required name="content"></textarea>
            </div>

            <!-- Send Button -->
            <button type="submit"
                class="w-full bg-purple-600 text-white py-4 rounded-xl font-semibold text-xl hover:bg-purple-700 transition-all duration-300 shadow-md">
                🚀 Send Message
            </button>
        </form>

        <!-- Divider -->
        <div class="flex items-center my-6">
            <hr class="flex-grow border-gray-300">
            <span class="mx-4 text-gray-500 text-lg">or</span>
            <hr class="flex-grow border-gray-300">
        </div>

        <!-- Login & Sign Up -->
        <div class="text-center text-lg">
            <p class="text-gray-500">
                Already have an account?
                <a href="/login" class="text-blue-500 font-semibold hover:underline">Login</a>
            </p>
            <p class="text-gray-500 mt-3">
                New here?
                <a href="/signup" class="text-blue-500 font-semibold hover:underline">Sign Up</a>
            </p>
        </div>
    </div>

</body>

</html>